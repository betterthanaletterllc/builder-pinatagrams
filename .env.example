# The hub's public read API (catalog + availability + pricing).
# Production: https://admin.betterthanaletter.com
# Local dev: the admin app runs on :3005 (cd ../admin && npm run dev).
NEXT_PUBLIC_HUB_URL=http://localhost:3005
